correct login errors

return task list after create/update/delete
global error handler
add "new" button at page "view task"
pressing "cancel" at "edit task" should return to "task-list" with saved scroll position

filter by tag at t-all
filter by tag at t-today

offline work

registration
native app
task-in-the-future
block only one line on edit (not whole table)
https
inline first loader
show age of task
bind tasks to project
create t-week
cyclic tasks

profile t-all to make it fast ?!

